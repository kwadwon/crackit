<%= form_for(@interview_session) do |f| %>
  <% if @interview_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interview_session.errors.count, "error") %> prohibited this interview_session from being saved:</h2>

      <ul>
      <% @interview_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
